apiVersion: v1
kind: Service
metadata:
  name: todos-api
  labels:
    app: todos-api
spec:
  type: ClusterIP
  selector:
    app: todos-api
  ports:
    - name: http
      port: 80
      targetPort: 8080

